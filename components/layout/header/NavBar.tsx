import logoWhite from 'public/logoWhite.svg';
import SVGContainer from '../../SVGContainer';

const NavBar = () => {
  return (
    <div>
      <SVGContainer svg={logoWhite} height={300} width={200} />
    </div>
  );
};

export default NavBar;
